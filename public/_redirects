# Netlify SPA fallback — every URL that doesn't map to a built file is served
# `index.html`, so React Router can take over client-side. Without this, deep
# links like /blog, /admin, /blog/<slug> all 404 on a fresh page load.
/*    /index.html   200
